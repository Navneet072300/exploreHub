export interface VideoResult {
  url: string;
  title: string;
  thumbnail: string;
  views: string;
  published: string;
  channel: string;
}

export interface RepoResult {
  url: string;
  name: string;
  stars: number;
  author: string;
  forks: number;
  language: string;
  description: string;
}
